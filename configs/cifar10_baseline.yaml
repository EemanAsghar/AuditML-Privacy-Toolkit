# Baseline training for CIFAR-10
experiment_name: "baseline_cifar10"
attacks: []

data:
  dataset: cifar10
  train_ratio: 0.5

model:
  arch: cnn
  num_classes: 10

training:
  epochs: 50
  batch_size: 64
  learning_rate: 0.001
  optimizer: adam
  seed: 42
  device: auto

dp:
  enabled: false
